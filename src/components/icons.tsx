// Thin re-export of the exact lucide icons referenced by name in the .pen design
// (each node there has `library: "lucide"`) so the app renders the real glyphs
// instead of hand-approximated paths.
export {
  Search as SearchIcon,
  Eye as EyeIcon,
  ArrowUpRight as ArrowUpRightIcon,
  Plug as PlugIcon,
  Unplug as UnplugIcon,
  Trash2 as TrashIcon,
  TriangleAlert as TriangleAlertIcon,
  ChevronDown as ChevronDownIcon,
  Pencil as PencilIcon,
  UserRound as UserRoundIcon,
  PanelBottom as PanelBottomIcon,
  LoaderCircle as LoaderCircleIcon,
  Hourglass as HourglassIcon,
  ArrowRight as ArrowRightIcon,
  Quote as QuoteIcon,
  Tag as TagIcon,
  Plus as PlusIcon,
} from "lucide-react";
